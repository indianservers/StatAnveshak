# Configuration

No environment variables are required for local use.

## Runtime Storage

- IndexedDB database: `AnveshakDB`
- localStorage preference keys use the `pref-` or `anveshak-` prefixes.

## Playwright

Playwright runs the Vite dev server on `http://127.0.0.1:5174`.

